{
  "$schema": "https://opencode.ai/config.json",
  "model": "deepseek/deepseek-v4-flash",
  "small_model": "deepseek/deepseek-v4-flash",
  "instructions": ["AGENTS.md"],
  "skills": {
    "refract-maintainer": true
  },
  "permissions": {
    "allow": [
      "read",
      "write",
      "edit",
      "bash(git *)"
    ],
    "deny": [
      "bash(npm *)",
      "bash(bun *)",
      "bash(node *)",
      "bash(python *)",
      "bash(pip *)",
      "bash(hatch *)"
    ]
  }
}
